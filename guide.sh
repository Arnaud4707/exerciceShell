#!/bin/bash

#find . -type f -excutable -delete
#find . -type f -name ".*.swp" -delete
#awk -F: '{if ($3 > 999 && $3 < 1010) { print $1 $3}}' /etc/passwd
#rm -r
#mv file path
#mv file new_file
#git log -n 5 --pretty=%Cgreen%H%n
#git ls-files -o -i --exclude-standard
